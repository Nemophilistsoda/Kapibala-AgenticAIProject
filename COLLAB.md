# COLLAB.md — 工作日志与决策记录

> **参赛形式：单人**（按群通知，允许一人一组）。
> 因此原题中「commit 历史要能看出两个人都有实质性提交」一条不适用；本文件相应地记录**单人工作过程中的关键决策、取舍与返工**。
>
> 这份文件也是我对题目那句「**考核的是你会不会用、会不会验证、会不会在 AI 给错答案时自己发现并纠正**」的回应。我不打算只在 README 里写一句「我们使用了 AI 并做了验证」——那不可验证。我的做法是：**让每一次「AI 给错 → 我发现 → 我纠正」都在 git 历史里留下可执行的证据**（一个失败的测试 + 一个让它变绿的修复），评委可以 `git checkout` 到修复前跑一遍，亲眼看到那个测试是红的。

---

## 1. AI 使用声明

本项目全程使用了 AI 工具（ChatGPT / Claude），**且不隐藏这一点**。使用方式是：

> **把 AI 的产出当作「提案」而不是「事实」，每一条都拿规格文档和测试去验。**

具体分工：

| 环节 | AI 做了什么 | 我做了什么 |
|---|---|---|
| 需求理解 | 给出对题目的初步抽象（LLM 负责语言理解、代码负责确定性约束） | 独立重读原题，逐条建 requirement matrix，标注 HARD/DERIVED/OPTIONAL/NOT_NEEDED（`docs/01`） |
| 仓库调研 | 推荐候选仓库 | **clone 下来读源码 + 跑 `py_compile`**，否掉了 AI 的第一推荐（`docs/02`） |
| 架构选型 | 倾向 LangGraph | 追问「LangGraph 在这题解决了什么真实问题」，逐条对照后**否掉**（`docs/03`） |
| 实现方案 | 每个关键问题给 2–4 种候选 | 逐条做 specification review，否掉 22 个（`docs/04`） |
| 编码 | 生成实现 | 拿 `docs/01` 需求矩阵 + `docs/05` 威胁模型逐条审，返工记录见下表 |

**诚实性边界**：下表只记录**真实发生过**的问题。设计阶段就识破、从未写进代码的坑，不伪装成「踩过又改了」——那些记在 `docs/04` 里，并在代码注释中留下「为什么不那样写」的说明，配一条证明「那样写真的会错」的测试。

---

## 2. 返工与纠正记录

> 每条对应 git 历史里的一对 commit。格式：问题 → 怎么发现的 → 怎么改的 → 哪条测试能证明。

| # | 问题 | 发现方式 | 修正 | 证明测试 | Commit |
|---|---|---|---|---|---|
| R-1 | 上一轮 AI（ZCode）声称完成 Stage-1 六份文档，实际一份都没落盘 | 不看它的汇报，直接 `ls docs/` | 重做 Stage-1 | — | — |
| R-2 | API key 明文在仓库根目录，而本次笔试要求 repo **public** | 核对群通知 + 检查根目录 | 遗留 key 文件由 `.gitignore` 排除；正式代码只读 `.env` / 环境变量；`.env.example` 只保留占位符 | `git check-ignore -v` + secret scan | — |
| R-3 | AI 推荐的第一候选仓库 `niti007/...` 实际 **import 不了**（`state.py` 有 SyntaxError），且用的是 OpenAI 不是 Gemini | `python -m py_compile src/*.py` + `grep import` | 该仓库降级为不采用 | — | — |
| R-4 | AI 称 `ntg2208/...` 有 SQLite 持久化 | 读 `graph.py` 源码 | 实际默认 `MemorySaver()`（纯内存），README 与实现不符，已记录 | — | — |
| R-5 | `.env.example` 被误写入真实 key，仍会被 `git add .` 纳入 public repo | staged-file 安全审查 | 换成 `your_gemini_api_key_here`，再次按文件名扫描高风险凭证 | secret scan | — |
| R-6 | 设计文档假设提供的凭证可直接用于 `google-genai`；真实 smoke test 返回 `401 ACCESS_TOKEN_TYPE_UNSUPPORTED` | 用官方 SDK 2.22.0 做一次最小分类调用 | 保持 FakeLLM 测试不阻塞；把真实凭证状态标为集成 blocker，等待确认 key 是否有效/绑定 Gemini API | live smoke test | — |
| _(待补)_ | | | | | |

---

## 3. 关键设计决策（题目没规定、由我拍板的）

这些都是**产品语义选择题**，任选其一都能自圆其说，但必须选定并能解释。详见 `docs/06` 第五部分。

| # | 决策 | 我的选择 | 理由 |
|---|---|---|---|
| D1 | action 由 LLM 建议还是代码映射 | LLM 提议，代码按 intent/action 兼容矩阵 clamp | 满足「Agent 选择」同时保留最终执行权；不兼容提议收紧为确定性 fallback |
| D2 | 哪些 action 算「主动发出的消息」 | 只有 `reply` | 其余三个动作都是内部状态变化；`schedule_followup` 题面明确「本轮不回复」 |
| D3 | `reply` 被限流后怎么办 | 降级为 `schedule_followup` | 不等待、不排队，避免状态过期和定时任务复杂度 |
| D4 | ESCALATED / CLOSED 下还调不调 LLM | 不调用 | 省 quota，并让锁定态的 prompt-injection 输入面为零 |
| D5 | reactivate 后 counter 是否清零 | 清零；CLOSED 保持静默 | 让人工接管真正恢复一段新的自动会话；终态不被客户文本解除 |

---

## 4. 主动砍掉了什么，为什么

> 题目原文：「时间紧张的话，**明确写清楚你砍掉了什么、为什么砍**，比硬撑一个不完整还讲不清楚的版本要加分得多。」

| 砍掉的 | 理由 | 如果要补，怎么补 |
|---|---|---|
| 跨进程 / 分布式并发 | 本题是单进程 CLI demo；题目二（全栈方向）才明确考真并发 | 把速率窗口移进 SQLite，用条件更新（`UPDATE ... WHERE last_sent_at < ?`）做 CAS |
| Web 前端 | 题目明说「越简单越好，重点不在这里」 | — |
| Agent 框架（LangGraph / ADK） | 四条硬约束没有一条是框架能替我满足的；且把「100% 强制」建立在框架回调时机上，我无法自证 | 见 `docs/03` |
| Function / tool calling | tool schema 限制的是「模型能提议什么」，不是「系统会执行什么」，对 100% 保证零贡献，还引入 AFC 版本风险 | 见 `docs/04` Q4 |
| RAG / 向量库 / multi-agent / Redis | 题目无此需求，只会扩大攻击面 | — |
| Streaming 输出 | 与「发送前拦截」冲突——流式已吐出的字收不回来 | 若要做，需在 token 级别做出站过滤，成本高 |
| _(待补)_ | | |

---

## 5. 时间投入

> 题目要求在 README 里注明实际花了多长时间。此处为明细，README 里给汇总。

| 阶段 | 投入 | 说明 |
|---|---|---|
| Stage-1 设计（需求逆向 / 调研 / 架构 / 威胁模型） | _(待记)_ | 产出 `docs/01`–`06` |
| Stage-2 实现 | _(待记)_ | |
| 测试（确定性 + 对抗） | _(待记)_ | |
| 文档 | _(待记)_ | |
| **合计** | _(待记)_ | |
